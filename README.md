# TapBeat

Ultra-light macOS BPM tapper. Spacebar (or click the LCD) to tap tempo; Esc or R to reset.

```bash
xcodebuild -project TapBeat.xcodeproj -scheme TapBeat -configuration Debug
open build/Build/Products/Debug/TapBeat.app
```
